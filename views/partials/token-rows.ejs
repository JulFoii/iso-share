<%
/*
 * Nur die Schleife ueber `apiTokens`, analog zu partials/file-rows — genutzt
 * von admin.ejs und von server.js' GET /admin/partials/listing, damit beide
 * exakt dasselbe Zeilen-Markup erzeugen.
 */
%>
<% apiTokens.forEach(token => { %>
  <%- include('token-row', { token }) %>
<% }); %>
