<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>Cookie-Einstellungen | ISO Share</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    html, body { height: 100%; background: #f0f2f5; }
    .wrapper { display: flex; flex-direction: column; min-height: 100%; }
    .content { flex: 1; }
    .footer { background-color: #343a40; color: #fff; text-align: center; padding: 15px 0; }
    .footer a { color: #adb5bd; text-decoration: none; margin: 0 10px; }
    .footer a:hover { color: #fff; text-decoration: underline; }
    .hero { background: linear-gradient(135deg, #007bff, #00d4ff); color: white; text-align: center; padding: 60px 0; }
  </style>
</head>
<body>
<div class="wrapper">
  <div class="content">
    <div class="hero">
      <div class="container">
        <h1 class="display-5 fw-bold">🍪 Cookie-Einstellungen</h1>
        <p class="lead">Kontrolle über optionale Cookies (TTDSG)</p>
      </div>
    </div>

    <div class="container my-5">
      <div class="row justify-content-center">
        <div class="col-lg-8">
          <div class="card shadow-sm">
            <div class="card-body p-5">
              <p class="text-muted">
                Diese Website nutzt <strong>technisch notwendige Cookies</strong> für die Admin-Session.
                Optionale Cookies (z. B. Analytics) sind standardmäßig deaktiviert.
              </p>

              <div class="border rounded p-3 mb-3">
                <div class="d-flex justify-content-between align-items-center">
                  <div>
                    <div class="fw-semibold">Notwendige Cookies</div>
                    <div class="small text-muted">Für Login/Session erforderlich. Immer aktiv.</div>
                  </div>
                  <div class="form-check form-switch">
                    <input class="form-check-input" type="checkbox" checked disabled>
                  </div>
                </div>
              </div>

              <div class="border rounded p-3">
                <div class="d-flex justify-content-between align-items-center">
                  <div>
                    <div class="fw-semibold">Analytics (optional)</div>
                    <div class="small text-muted">Nur aktiv, wenn du zustimmst.</div>
                  </div>
                  <div class="form-check form-switch">
                    <input class="form-check-input" id="analytics" type="checkbox" <%= cookieConsent.analytics ? 'checked' : '' %>>
                  </div>
                </div>
              </div>

              <div class="d-flex gap-2 mt-4">
                <button class="btn btn-outline-secondary" id="reject">Nur notwendige</button>
                <button class="btn btn-primary" id="save">Speichern</button>
                <a href="/" class="btn btn-link ms-auto">Zur Startseite</a>
              </div>

              <div class="alert alert-success mt-4 d-none" id="saved">✅ Einstellungen gespeichert.</div>
              <div class="small text-muted mt-3">
                Mehr Infos findest du in der <a href="/datenschutz">Datenschutzerklärung</a>.
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <footer class="footer">
    <div class="container">
      <p class="mb-0">© <%= year %> ISO Share. Alle Rechte vorbehalten.</p>
      <div class="mt-1">
        <a href="/datenschutz">Datenschutz</a>
        <a href="/impressum">Impressum</a>
        <a href="/cookie-settings">Cookies</a>
      </div>
    </div>
  </footer>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script>
  window.__SHOW_COOKIE_BANNER__ = <%= showCookieBanner ? 'true' : 'false' %>;
</script>
<script src="/cookie-consent.js"></script>
<script>
  function api(path, data) {
    return fetch(path, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data || {})
    }).then(r => r.json());
  }

  const saved = document.getElementById('saved');
  const analytics = document.getElementById('analytics');

  document.getElementById('save').addEventListener('click', () => {
    api('/api/cookie-consent', { analytics: analytics.checked }).then(() => {
      saved.classList.remove('d-none');
      setTimeout(() => saved.classList.add('d-none'), 2000);
    });
  });

  document.getElementById('reject').addEventListener('click', () => {
    api('/api/cookie-consent/reject').then(() => {
      analytics.checked = false;
      saved.classList.remove('d-none');
      setTimeout(() => saved.classList.add('d-none'), 2000);
    });
  });
</script>
</body>
</html>
